const { Mode3x3 } = require('./Mode3x3');
const {
    ModeHearts,
    ModeClubs,
    ModeSpades,
    ModeDiamonds,
} = require('./ModesRace');

const {
    ModeUp,
    ModeDown,
} = require('./ModesDirectional');

const {
    ModeSlalom,

} = require('./ModeSlalom');

const {
    ModeTannenbaum,
} = require('./ModeTannenbaum');

const {
    ModeJoker,
} = require('./ModeJoker');

module.exports = {
    Mode3x3,
    ModeHearts,
    ModeClubs,
    ModeSpades,
    ModeDiamonds,
    ModeUp,
    ModeDown,
    ModeSlalom,
    ModeTannenbaum,
    ModeJoker,
}